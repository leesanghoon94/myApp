apps:
  front:
    enabled: true
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
      averageUtilization: 80
      targetMemoryUtilizationPercentage: 70
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
    image:
      repository: leesanghoon94/front-eks
      # Overrides the image tag whose default is the latest.
      tag: "60db3"
  server:
    enabled: true
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
      averageUtilization: 75
      targetMemoryUtilizationPercentage: 80
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
    image:
      repository: leesanghoon94/server-eks
      # Overrides the image tag whose default is the latest.
      tag: "60db3"
mysql: true
ingress:
  subnets: subnet-007271349f0e2738e, subnet-098629dccf8a81003
